CQ5 AND SUPER TYPE COMPONENT COPY


You can create virtual copy of component. So you shouldn`t
copy all folder with component but only indicate it as
super type. Then if you want to change something you
just override it in your local folder. For instance:
dialog.